package com.steply.app.sync

/** Transport/storage operating limits. These are not clinical cutoffs. */
object Stage5DataConfig {
    const val RECENT_ASSESSMENT_LIMIT = 5
    const val LANDMARK_TARGET_FPS = 30
    const val LANDMARK_MAX_SAMPLES = 2_700
}
